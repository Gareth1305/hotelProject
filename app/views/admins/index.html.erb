<h1>Administrators</h1>

<table>
  <tr>
    <th width="150"><u>Email Address</u></th>
    <th width="350"><u>Password</u></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @admins.each do |admin| %>
  <tr>
    <td><center><%= admin.adminEmail %></center></td>
    <td><center><%= 'Passwords are hidden for security reasons' %></center></td>
    <td><%= link_to 'Show', admin %></td>
    <td><%= link_to 'Edit', edit_admin_path(admin) %></td>
    <td><%= link_to 'Destroy', admin, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Admin', new_admin_path %>
